# ================================
# SERVER CONFIGURATION
# ================================
PORT=5001
NODE_ENV=development

# ================================
# OCR SERVICE CONFIGURATION
# ================================
# OCR API endpoint
OCR_SERVICE_URL=http://4.236.205.190:8000/api/v1/new_agentic_ocr

# OCR API key (leave empty if not required)
OCR_API_KEY=

# OCR request timeout (milliseconds)
OCR_TIMEOUT=90000

# OCR blob URL (sample document for testing)
OCR_BLOB_URL=https://superagentsa.blob.core.windows.net/testing/ProcCodes_71971708300.png

# OCR extraction prompt
OCR_PROMPT=Extract all fields in json

# OCR models (primary and fallback)
OCR_MODEL=gemini-2.5-flash
OCR_MODEL_ALT=Qwen2-VL-72B-Instruct

# ================================
# DATABASE CONFIGURATION
# ================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/arcelordb?schema=public"

# ================================
# FILE UPLOAD CONFIGURATION
# ================================
# Maximum file size (bytes) - 10MB default
MAX_FILE_SIZE=10485760

# Allowed file types (comma-separated)
ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf,text/plain

# ================================
# LOGGING CONFIGURATION
# ================================
# Log level: error, warn, info, debug
LOG_LEVEL=info

# ================================
# NOTES
# ================================
# 1. Copy this file to .env and update with your actual values
# 2. Never commit .env to git (it's in .gitignore)
# 3. OCR_API_KEY is optional - leave empty if your service doesn't require authentication
# 4. DATABASE_URL must be updated with your PostgreSQL credentials
